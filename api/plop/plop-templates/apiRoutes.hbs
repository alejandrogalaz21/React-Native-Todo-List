import { Router } from 'express'
import * as {{noun}}Controller from './{{noun}}.controller'
// * import validators
// * import middleware 


const router = new Router()

//* C.R.U.D ROUTES *//

/**
 * @desc      get all the records paginated
 * @access    Public
 * @route     GET api/{{noun}}
 * @params    query, page, limit
 */
router.get('/{{noun}}', {{noun}}Controller.getAll{{pascalCase noun}}Pagination)

/**
 * @desc      get all the records
 * @access    Public
 * @route     GET api/{{noun}}
 * @params    queryString, page, limit
 */
router.get('/{{noun}}/all', {{noun}}Controller.getAll{{pascalCase noun}})

/**
 * @access    Private
 * @route     POST api/{{noun}}
 * @desc      create a record
 * @params    {payload}
 */
router.post('/{{noun}}', {{noun}}Controller.create{{pascalCase modelName}})

/**
 * @desc      get single record
 * @access    Private
 * @route     GET api/{{noun}}/id
 * @params    id
 */
router.get('/{{noun}}/:id', {{noun}}Controller.getOne{{pascalCase modelName}})

/**
 * @desc      update a record
 * @access    Private
 * @route     PUT api/{{noun}}/id
 * @params    id, {payload}.
 */
router.put('/{{noun}}/:id', {{noun}}Controller.update{{pascalCase modelName}})

/**
 * @desc      delete a record
 * @access    Private
 * @route     DELETE api/{{noun}}/id
 * @params    id
 */
router.delete('/{{noun}}/:id', {{noun}}Controller.delete{{pascalCase modelName}})

// TODO work in progress
//* OTHER ROTES (EXPORT, REPORTS, ETC...) *//

/**
 * @desc      export to cvs the selected rows
 * @access    Private
 * @route     POST /{{noun}}/export/cvs
 * @params    {rows}
 */
router.post('/{{noun}}/export/cvs', (req, res) => {
  const rows = req.body
  console.log('{{noun}} exort to cvs')
  console.log({ rows })
  return
})

export default router
